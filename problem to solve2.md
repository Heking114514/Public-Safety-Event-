# problem to solve 2：以完赛为第一目标的工程问题清单

更新时间：2026-09-12
审查对象：当前工作空间中的导航、融合、IMU、轮速、串口、规划器、ORB-SLAM3 wrapper 和启动脚本。

## 0. 先把原则说清楚

这是比赛车辆，不是实验室里的论文系统。比赛中没有人能走进去给车纠偏，所以第一目标是：**只要车还有可信的位置、还能控制，就尽量继续走；不确定时先降速或短暂停车重试；不要因为一次抖动、一次丢帧或一个普通告警就结束任务。**

这里的“安全”不是看到任何异常都停。对本项目来说，更实用的安全顺序是：

1. 继续正常跑。
2. 继续跑，但降速、减少动作幅度。
3. 零速短暂停车，保留路线和航点，自动等恢复后继续。
4. 尝试有限次数的重新对齐、重新规划或重新连接。
5. 只有确认没有可信定位、没有可控执行器、车辆确实被卡住，或者继续运行会明显把车带到未知区域时，才锁存故障。

不能接受的两种极端：

- 把所有异常都变成 `FAULT`，车很“谨慎”但比赛直接结束。
- 为了不停，把所有检查都关闭，车失去位置后继续盲冲。

### 四种动作的统一含义

- **继续**：继续发布非零速度，按当前路线前进。
- **降速**：仍然发布非零速度，但降低线速度或角速度；原地转向也归在这里。
- **停车恢复**：发布零速，任务状态、路线和航点索引保留；条件恢复后自动继续。
- **人工介入**：任务被关闭或故障锁存，需要修复设备、重启节点、重新规划或重新发送 `start`。这应该是最后一步，不应作为普通抖动的默认反应。

## 1. 当前最值得先处理的问题

### P0-01 [已解决] ORB 暖机时 fusion 先报 `FAULT`，可能启动即锁死

相关代码：`src/fused_odometry/src/fusion_logic.cpp:524-565`、`src/fused_odometry/src/health.cpp:5-62`、`src/visual_navigation/include/visual_navigation/navigation_supervisor.hpp:157-205`。

现状：

- ORB 相机默认先暖机约 2 秒，并且要累计若干个连续有效视觉增量后才算真正接收过视觉。
- 在此之前 `visual_.ever_accepted=false`。
- `select_mode()` 对 `initialized=false` 直接返回 `FAULT`。
- fusion 每 0.1 秒把这个状态发布出去。
- 导航 supervisor 把它当成已经发生的硬故障；启动时还没有“曾经有效定位”的历史，不能使用短暂丢定位宽限。

实际结果：只要 CSV `autostart`、动态规划路线或其他入口在 ORB 暖机期间激活了路线，导航可能在第一帧有效视觉到来前就停车并锁存。等 ORB 约 2 秒后恢复也不会自动继续。

这是明确的状态设计错误，不是正常的安全保护。应增加独立的 `WAITING_FOR_INITIALIZATION`（或同等状态）：

- 初始化阶段只停车等待，不锁存；
- 初始化超过明确上限后才转成 `FAULT_INIT_TIMEOUT`；
- 已经成功定位过以后再出现真正的 `FAULT`，仍然锁存；
- 导航只有收到“已初始化且允许运行”才开始发速度。

不要简单给所有 `FAULT` 加宽限，否则会放过运行中的真实定位故障。

验收标准：路线在相机刚启动时已激活，车保持零速等待；ORB 产生连续有效样本后无需人工再次 `start` 就能继续。

处理状态：本轮已修复。初始化等待与运行中真实故障分离，并已通过融合、导航及工作区测试。

### P0-02 [已解决] 转向稳定计时器会被每周期清零

相关代码：`src/visual_navigation/include/visual_navigation/turn_settle_controller.hpp:21`、`src/visual_navigation/src/path_following.cpp:62-110`、`src/visual_navigation/src/maneuvers.cpp:53-132`。

现状：yaw 误差进入容差后，`begin_settling()` 在 30 Hz 的每个周期都会执行，而该函数会把内部计时器重新置为未开始。结果是要求的 `turn_settle_dwell=0.30 s` 可能永远达不到。

影响：

- 车已经基本转到位，却迟迟不能进入下一状态；
- 随后可能被 `TurnProgressSupervisor` 误判为转向无进展；
- 最终出现 `FAULT_NO_TURN_PROGRESS`，看起来像底盘坏了，实际是状态机没有完成稳定判定。

修复方向：`begin_settling()` 应该是幂等的，已经 settling 时不能重置开始时间；只有重新离开角度容差或明确 `reset()` 时才清零。补一个真实 30 Hz 连续调用测试，不能只测试“调用一次 begin”。

处理状态：本轮已修复。稳定计时器改为幂等，已补连续周期测试并通过。

### P0-03 [已解决] IMU 出现一次大时间间隔后可能永久停更

相关代码：`src/imu_rpy_filter/src/update.cpp:26-33`。

现状：当 `dt > 0.1 s` 时直接 `return`，但没有更新 `last_stamp_`。后续消息仍然拿旧时间戳计算 dt，可能每次都被判为过大，直到重启节点才恢复。

影响：IMU 看似还在发布，实际上滤波器不再更新；融合会逐渐降级、停车，甚至把后续问题误判成视觉或底盘故障。

实用修复：丢弃大间隔样本时同步重置时间基线和必要的滤波状态，让下一帧重新建立基线；不要因为一次 USB/调度抖动让整场比赛的 IMU 永久失效。需要测试“丢一帧后下一帧恢复”。

处理状态：本轮已修复。异常间隔会重建时间基线，后续样本可恢复更新；倒序、重复、负秒数、非法纳秒字段和非有限时间戳也有测试覆盖，坏包不会打崩滤波节点。

### P0-04 [已解决] 路径无进展监督对“完全不动”反而不报警

相关代码：`src/visual_navigation/include/visual_navigation/path_progress_supervisor.hpp:58-124`、`src/visual_navigation/src/progress.cpp:3-29`。

当前参数：10 秒内沿线进度至少增加 `0.05 m`，并且窗口内实际位移达到 `0.25 m`，才会触发一次恢复；最多两次，第三次进入 `FAULT_NO_PATH_PROGRESS`。

当前动作：

- 第一次、第二次：发零速，清 PID/锚点，下一周期在同一航点重新尝试；
- 这不是脱困动作，没有倒车、换方向或重新规划；
- 第三次：关闭导航、持续零速、需要人工重新 `start`。

问题：如果车真的被墙、线缆或底盘卡住，位置始终没有达到 `0.25 m`，监督器会一直停在 monitoring，可能永远不进入故障。相反，轮胎打滑或定位漂移达到 `0.25 m` 时，可能误触发恢复。

应增加一条独立规则：**已经持续发出明显运动命令，但实测线速度/位移接近零超过限定时间，就进入短暂停车和重试**。不要把 `0.25 m` 作为唯一的失速前提。恢复时至少要重新获取定位、检查执行器反馈，必要时做一次小幅可控的重新对齐或局部重规划。

处理状态：本轮已修复。新增持续运动命令但实际低运动的独立监督，前两次自动停车恢复，第三次才锁存 `FAULT_NO_PATH_PROGRESS`；已补测试并通过。

### 设计决策 D-01：轮速不进入 EKF 是正确的工程取舍，失视觉能力要单独标定

相关配置：`src/fused_odometry/config/fused_odometry.yaml:138-169`、`src/fused_odometry/src/fusion_logic.cpp:541-565`。

当前配置明确把 `/fusion/input/wheel_odom` 排除在 `fused_ekf` 之外，轮速只用于残差、打滑/失速监督和有限的失视觉距离预算；`fuse_wheel_yaw=false` 也避免把已知偏大的转向尺度灌进状态估计。

这不是缺陷，而是符合当前车辆实际情况的正确取舍：**轮式里程飘得很厉害时，不能因为“有一个传感器”就给它很高置信度，更不能让它把视觉里程计带偏。** 强行融合轮速会让全局位姿和路线判断更差，宁可少用也不要污染主状态。

但“不进 EKF”不等于“轮速完全没有影响”。视觉正常时，`select_mode()` 明确忽略轮速好坏，轮速异常只留下告警；视觉失效时，健康监视器仍用轮速速度累计失视觉距离，并据此决定还能不能继续。轮速高估会让车过早停下，轮速低估则可能让车盲走得太远，这属于失视觉策略的边界风险，不是把轮速重新塞回主状态就能解决的。

当前 `fuse_wheel_yaw=false`，所以默认配置下“只有 wheel、没有视觉和 IMU”的 `DEGRADED_WHEEL_ONLY` 实际不会被放行，最终会进入 `FAULT`。只有在重新做过转向标定、确认短时 fallback 值得开启时，才应单独打开它；不能把这个状态名当成已经验证的能力。

上位机参数错配已按 2026-09-12 实测值处理：当前运行 YAML 和代码兜底值均使用轮径 `47.6 mm`（半径 `0.0238 m`）、轮距 `0.1247 m`，左/右每圈暂时继续使用 `1060.1667/1060.9333` 计数，串口仍为 `115200`。旧底盘的距离比例和 `yaw_slip_scale=0.448` 已停用，避免把旧标定误套到新物理模型；新的打滑补偿必须以后续实车 bag 为准。

仍有一个实车部署项没有完成：下位机仓库提交 `d3f022a` 的 `Core/Inc/chassis.h` 还是旧半径 `0.0313 m` 和旧轮距 `0.1408 m`。下次编译刷写固件前必须同步成 `0.0238 m / 0.1247 m`，否则上下位机仍会按两套运动学运行。

真正需要确认的是能力边界：失视觉时状态机可能仍允许短时间降速继续，但如果 EKF 没有轮速位置输入，车辆到底能靠 IMU/局部状态维持多久，不能只看 `DEGRADED_NO_VISION` 这个名字。应在实车上测量隧道/遮挡段的实际漂移，再决定继续速度和时间/距离预算；必要时用更保守的命令距离上限做兜底。不要为了“看起来完整”把飘移轮速重新塞回正常融合。

### P0-06 [已解决] 规划器可能漏覆盖或报告假成功

相关代码：`src/arena_path_planner/src/planner.cpp`、`src/arena_path_planner/src/node.cpp`、`scripts/arena_route_frontend.py`。

已发现的实际风险：

- 当前生产图的多阶段规划曾漏掉 `ROAD_97_108`、`ROAD_185_196`，但仍可能报告 `all_targets_reached=true`；
- 没有剩余边或传入错误的 `covered_edges` 时，layer3 可能返回单点路线并报告成功，导航随后立即 `GOAL_REACHED`；
- 所有目标到达但回起点 A* 失败的异常路径可能被吞掉，仍返回成功；
- `tunnel_segments` 主要约束目标顺序，不是严格的几何穿越约束；
- `minimum_turning_radius` 主要影响 smoothing，不是 A* 的硬约束；
- `tracking_margin` 是成本项，不是绝对安全边界。

这不是“多停几次”的问题，而是可能让车以为任务完成、实际却没有覆盖赛道。比赛前必须用独立脚本核对所有道路边、隧道进出和最终回程；无剩余任务时明确返回“已完成”，不能伪造一条单点成功路线。

处理状态：本轮已修复。三阶段路线会按最终实际几何重新核对覆盖结果，规划后处理也会保留已经巡检道路的端点；生产图中原先漏掉的 `ROAD_97_108` 和 `ROAD_185_196` 已由真实路线覆盖。没有剩余道路时，在起点会明确返回“已完成、无需新路线”，不在起点则规划安全回程。后续阶段暂时不可达时，不再丢掉前面已经规划好的安全路线：只要路线确实有位移、从本次请求起点出发、通过碰撞检查，而且能新增任务、隧道或道路进度，就允许先执行这一段。隧道必须同时到达入口和出口才记为完成；仅仅绕行到道路附近不能冒充道路覆盖。

前端不会在规划成功时提前记账。只有本次路线收到匹配的 route ACK、导航确实进入运行状态并最终报告 `GOAL_REACHED`，才提交该段实际进度；随后用最新融合位姿、已完成道路和 `remaining_visits` 自动规划下一段。这样既不会因为一时没有完整路线而停车，也不会把没跑过的任务写成完成。

### P0-07 [已解决] 实车入口必须确认真的启用了执行器健康门禁

导航配置默认 `require_actuator_health=false`，统一 serial bringup 会强制打开，但普通 bringup 或手工启动可能没有这个门禁。执行器只连通、MCU 模式正常，不代表轮子真的按命令转。

如果比赛入口没有使用 serial bringup，车辆可能在轮子失效时继续消耗路线和时间。应把比赛启动入口固定下来，并在启动日志中明确打印 `require_actuator_health=true`。

处理状态：本轮已修复。统一启动脚本在使用串口时固定选择 serial bringup，并显式传入、打印和记录 `require_actuator_health=true`；无串口的上位机开发模式才保留 `false`。运行中执行器状态丢失或不健康时，导航停车并保留路线，状态恢复后自动续跑。

### P0-08 [已解决] planner 对外部输入的 finite 校验不完整，异常请求可能让进程崩溃

相关代码：`src/arena_path_planner/src/node.cpp:143-186`、`src/arena_path_planner/src/grid.cpp:78-83`、`src/arena_path_planner/src/config.cpp:29-35`。

`HandlePlan()` 直接把请求里的起点、四元数和目标坐标交给 planner，没有先确认它们是有限数，也没有确认四元数范数有效。NaN/Inf 进入 `floor()` 再转 `int`，可能触发未定义行为；配置文件里的点和部分数值也没有统一 finite 检查。planner 服务回调异常时节点可能直接退出，比赛任务随即中断。

这不是要求“输入必须完美”的学术洁癖，而是要把坏请求变成一次可恢复的规划失败：返回 `success=false`、保留旧路线并告警，不能让一个错误消息杀掉 planner 进程。输入校验应集中在服务入口和配置加载入口，失败时给出清楚原因。

处理状态：本轮已修复。起点、目标、四元数、配置数值和栅格转换都增加了有限值及范围检查；四元数会先正规化，零范数会被拒绝。服务回调有顶层异常兜底，坏请求只返回 `success=false` 并告警，不退出 planner、不发布新路线，导航继续保留上一条有效路线。

### P0-09 [已解决] coverage 的 A* 失败分支可能发布不安全路线或假完成

相关代码：`src/arena_path_planner/src/coverage.cpp:232-242`、`src/arena_path_planner/src/coverage.cpp:356-466`。

当前有两类危险分支：

- 安全连接的 A* 失败时，代码仍把原目标点直接 `push_back` 到 `safe_points`，后续可能把一条未经验证的直线交给导航；
- coverage 的完整 tour 不存在时 `best_state` 可能保持负值，但后续仍可能把结果设为 `success=true`，并因 `deferred_targets` 为空而报告 `all_targets_reached=true`。导航会把单点或不完整路线当成任务完成。

这两条必须改成“明确失败或明确 deferred”，不能为了让 planner 不抛异常而把不安全点继续输出。所有最终发布的相邻路径点都要再次通过 `SegmentIsFree`/footprint 检查；没有完整覆盖路线时不能返回完整成功。

处理状态：本轮已修复。A* 无法修复连接时会明确失败，不再硬塞目标点；找不到完整 tour 时不会返回完整成功，回起点失败会加入 `RETURN_TO_START` 延迟项。所有最终相邻路径段都会再次做 `SegmentIsFree` 检查；空路线、单点路线、起点不匹配或没有新增任务进展的路线不能激活导航。有延迟目标本身不再是停车理由：已验证安全且有新增进展的阶段路线可以激活，跑完后自动从新位置续规划。服务响应使用独立的 `navigation_activated` 表示路线是否真的发布，不能再用 `success` 猜测车辆是否已经收到路线。

### P0-10 [已解决] 动态障碍和导航 odom/IMU 的输入边界还不够可信

planner 把动态多边形压成 AABB，有限的普通多边形会被保守放大，但退化的点/线障碍会被直接忽略，NaN 点也没有被拒绝；这可能让实际挡路物没有进入规划。导航 `HandleOdometry()` 允许空 `frame_id`，也不检查 `child_frame_id`，来自错误坐标系的数据可能被当成合法位姿。

比赛策略上，不能因为一个障碍消息格式不好就终止整场任务；应丢弃该障碍并告警，同时保留上一条已验证路线。对于已经确认的前方障碍，要靠在线占用检测停车/重规划，而不是依赖 planner 的一次性 AABB 转换。

处理状态：本轮已修复。有限的点或线障碍会扩成至少一个栅格大小后再参与膨胀；空多边形或 NaN/Inf 会使本次规划失败，但 planner 不退出、旧路线不被替换。导航和规划前端都只接受新鲜、时间戳严格递增、明确的 `map -> base_link`、有限 XY 和有效四元数；单个坏 odom 直接丢弃，不覆盖上一有效位姿，也不推进时间戳基线。odom、IMU 和动态路线的负时间戳或非法纳秒字段都会在构造 ROS 时间对象前丢弃，不能再用一个坏包打崩导航进程或替换旧路线。短时缺少合法输入会沿用现有的定位丢失降速宽限，宽限耗尽后停车等待；下一条合法数据到来后自动恢复。

## 2. 运行中会影响完赛的主要问题

### P1-00 [接口已留，感知待实现] 突发静态障碍必须由 YOLO 确认并用深度测距

不能把深度图中央出现几个近点直接当成可靠障碍识别：窄路侧墙、地面、车体安装偏差都可能触发误停车。当前上位机只保留 `/obstacle/front_blocked` 和 `/obstacle/front_range` 两个输入接口，以及收到确认后停车、沿实际来路倒回路口、请求重规划的执行逻辑；默认比赛启动不运行障碍感知生产者。

待实现的独立感知模块应先用 YOLO 确认需要避让的目标，再从同一目标框内取对齐深度并发布距离。导航始终订阅这两个话题，没有发布者或没有新消息就按“无障碍”处理，不再设置额外启用开关。识别类别、置信阈值、目标框深度统计和时序一致性都属于该模块，不能塞进规划器或导航控制器。

### P1-01 `FAULT_NO_PATH_PROGRESS` 的恢复太像“重试同一个错误动作”

两次恢复只是停车和 `ResetManeuver()`。如果原因是临时控制器积分、锚点或一次定位跳变，这有机会自愈；如果原因是实体障碍、底盘卡死或路径本身不对，重复同一动作没有意义。

实用做法：第一次恢复可只 reset；第二次恢复应增加重新获取定位、换一个短距离目标或请求局部重规划。只有这些动作仍无效才锁存。不能无上限重试，也不能把两次 reset 当成真正脱困。

### P1-02 `FAULT_NO_TURN_PROGRESS` 的判定可能被传感器和 settling 问题误触发

默认是 yaw 误差 6 秒内没有减少至少 `0.08 rad`，最多两次恢复，第三次锁存。6 秒不是固定总时限：只要误差持续改善，计时会刷新；另有约 12～40 秒的动态总时限。

应区分三种情况：

- yaw 传感器不更新：先停车等待/切换可信来源；
- 命令没到执行器：走执行器恢复；
- 命令到了但底盘确实不转：才算机械转向故障。

在修好 P0-02 前，不应把所有 `FAULT_NO_TURN_PROGRESS` 都认定为底盘故障。

### P1-03 [已解决] 航点刹车被视觉回摆误判为未停稳

相关代码：`src/visual_navigation/include/visual_navigation/waypoint_brake_controller.hpp:39-65`、`src/visual_navigation/src/maneuvers.cpp:22-50`。

2026-09-12 实车 bag 中三次 `FAULT_WAYPOINT_BRAKE_TIMEOUT` 均不是硬件损坏：MCU 已收到零速，左右轮目标和实测最终均为零，PWM 为零，串口、执行器和 ORB 跟踪正常。旧代码却用融合位姿 `0.20 s` 窗口判停；静止时几毫米视觉回摆被换算成 `0.016~0.036 m/s`，超过 `0.03 m/s` 阈值，于是在 `0.60 s` 后错误结束任务。

处理状态：本轮已解决。

- 停车确认优先使用刹车开始后收到的新鲜 MCU 双轮瞬时速度，只判断是否归零，不把轮式里程加入位置融合；
- MCU 遥测超过 `0.30 s` 或刹车后尚无新样本时，回退融合位姿速度；
- 单次确认时间窗放宽到 `1.00 s`；超时继续发零速并进入 `RECOVERING_WAYPOINT_BRAKE` 自动重试，不关闭任务；
- MCU 明确报告轮子仍在转时不能开始转向；
- 动态路线首点已在容差内且没有停留要求时直接跳过，避免毫米级短段造成一次假转角刹车。

仍需实车复跑确认最坏地面、载荷和低电量工况，但即使确认超时也不会再因普通视觉噪声终止整场任务。

### P1-04 短暂定位异常目前会以 25% 速度继续，但坐标是否仍可信要验证

导航有约 2 秒的 transient localization grace，期间以 `transient_fault_speed_scale=0.25` 继续，并暂停路径/转向进度监督。这个策略有利于穿过短暂丢帧，但如果坐标已经跳变，车会拿旧路线坐标继续走。

实用策略：只有在位移、yaw 和命令方向仍连续时才允许这段低速桥接；发现明显跳变就停车重定位。不要把所有 `FAULT` 都桥接，也不要在没有位姿的情况下盲跑整条路线。

### P1-05 视觉、轮速和编码器异常的反应不一致

已确认的行为：

- 视觉正常、轮速与视觉不一致时，通常只是告警，导航仍继续；这有利于完赛，但要确保视觉确实可靠；
- 视觉丢失时，轮速 gate 可能直接把状态推到 `FAULT`；
- 重复编码器 sequence 不刷新 freshness，最终轮速过期；
- 旧三字段 `ENC,left,right` 会被补成四字段，但 sequence 可能一直为 0，后续被当成重复帧；
- sequence 有缺口通常继续并增大不确定度；时间倒退或 tick 跳变会重建 baseline；
- 低于检测门槛的速度不会触发机械失速检测。

需要统一成“单次坏帧丢弃，短时降级，连续确认后才停车”的规则；同时修正旧编码器协议兼容，不要让兼容代码制造永久无轮速。

### P1-06 机械失速检测有门槛，低速卡死可能漏检

直线命令约 `0.08 m/s` 以上且约 `0.60 s` 无运动，或原地转向命令约 `0.55 rad/s` 以上且约 `0.80 s` 无角速度，才会进入 `FAULT_STALLED`。低于这些门槛时依赖路径进度监督，而路径监督又有 `0.25 m` 位移门槛。

因此“低速慢慢顶住不动”可能两个检测都不触发。应增加低速但持续命令的较长超时，而不是只提高灵敏度到会误报正常低速动作。

### P1-07 串口恢复有完整流程，但耗尽两次后直接锁存

串口不存在、读写失败、CTL 重复/乱序、MCU 非导航模式、急停或 `/cmd_vel_nav` 超时，通常会先发零速并每秒重连；执行器跟踪异常持续约 1.5 秒后自动恢复，零速约 0.5 秒后最多重试两次，连续正常约 3 秒清除历史。

两次恢复耗尽后进入 `LATCHED`，需重启/重连串口节点。这个上限可以保留，但第二次重试前应确认设备是否真的重新打开、命令是否重新开始流动；否则只是重复等待，浪费比赛时间。

另一个明显缺口是串口桥只拒绝 NaN/Inf，没有限制有限但极大的速度值。上层任何异常数值都应在桥接层夹紧到车辆允许范围。

### P1-08 动态路线失败时旧路线可能继续执行

空路线、错误 frame、零时间戳、非法 pose 会被忽略；planner 服务失败时也可能不主动取消旧路线。这种“保持旧任务”有时能保住完赛机会，但如果旧路线已经不再适合当前环境，就会悄悄继续错误方向。

实用处理：

- 能确认新路线只是坏消息时，保留旧路线并继续，同时发布明确告警；
- 已确认旧路线失效时，停车等待重规划；
- 路线发布要有 ID、接收确认、激活确认和失败原因；
- 一次性可靠 QoS 消息要支持发布重试，避免启动顺序导致路线丢失。

### P1-09 planner 没有运行中的动态障碍处理

动态障碍只参与本次规划。路线开始后出现人、箱子或移动障碍，导航没有在线占用检测、自动停车或局部绕行。

比赛中不能指望人工进去搬车。至少要有“前方明显不可通行时低速停车，短暂等待后重新规划”的闭环；如果没有传感器支持，不要假装已经具备动态避障能力。

### P1-10 frame、时间戳和容差口径仍不统一

已发现：planner 输出 frame 为 `map`，occupancy grid 使用 `arena`，工程没有自动提供两者 TF；planner 任务容差约 `0.08 m`，navigator 默认航点容差约 `0.04 m`；仿真时间没有 `/clock` 时路线时间戳可能为 0 并被忽略。

这些不是理论问题，会表现为“规划说到了，导航说没到”或路线根本没被接收。比赛配置必须统一 frame、时间源和成功容差，并在启动时检查 TF 是否存在。

### P1-11 ORB 丢跟踪后的新原点可能使旧路线失效

ORB 丢跟踪后重新建立原点时，旧地图坐标和新位姿坐标未自动对齐。此时继续沿旧路线跑，可能把车带离实际赛道。

建议：短时丢跟踪只低速等待恢复；发生重新初始化/原点变化时自动暂停路线，重新获取当前位姿并局部重规划，不要把“重新定位成功”误认为“仍在原路线坐标系”。

### P1-12 进程退出后主要依赖人工重启

ORB、fusion、导航或串口进程退出时，脚本通常发零速并停止剩余栈，但没有完整的自动拉起和状态恢复闭环。比赛中进程偶发退出会直接结束任务。

建议至少由启动监督器自动重启可恢复节点，并保留路线、当前航点和 hold 状态；连续重启失败才锁存。底盘 watchdog 仍必须独立存在，不能只靠上位机。

### P1-13 启动、手动 start 和动态路线的行为不完全一致

当前没有路线时是 `WAITING_FOR_ROUTE`，路线文件打不开或格式错误时是 `FAULT_ROUTE_NOT_LOADED`；合法路线在 `autostart=false` 时停在 `IDLE`，需要人工发送 `start`。手动 `start` 在定位/执行器不健康时会被拒绝，条件恢复后不会自动再次 start。

这些本身不算 bug，但比赛中容易被误解为“系统没反应”。启动脚本、planner GUI 和动态路线入口应统一一个规则：

- 初始化和等待数据时自动等；
- 真正的路线错误才提示人工修文件；
- 只是暂时不健康时保持路线，恢复后自动重新尝试启动；
- `start` 被拒绝要有明确状态，不要让操作者猜。

### P1-14 过期/未知融合状态的处理仍偏硬

导航对 fusion status 过期、未允许、未知或 `FAULT_*` 的处理主要是零速；已初始化后的真正 fault 锁存是必要的，但启动前未初始化、短时状态丢失和已经确认的运行故障不应共用一条路径。

建议按“未初始化、短暂过期、已降级、确认故障”分别计时。短暂过期时可短时降速或停车等待，连续超过预算再锁存；不要因为一次 status 消息延迟就结束任务。

### P1-15 [已解决] 转向位置保持函数可能带出小幅反向线速度

路径对齐和最终 yaw 对齐使用 `TurnPositionHoldSpeed()`。当车辆相对锚点发生反向误差时，该函数可能给出小幅负 `linear.x`。文档意图是“原地转向时不倒车”，实现和描述不完全一致。

比赛场地狭窄时这会让车在转向过程中向后蹭，影响路线和碰撞风险。三次 2026-09-12 实车 bag 进一步证明，小幅 `linear.x` 还会越过下位机 `0.01 m/s` 的原地转向门槛，让底盘从专用转向闭环掉回普通轮速 PI。

处理状态：本轮已解决。路径对正、最终 yaw 对正和恢复过程中的纯转向全部强制发布严格为零的 `linear.x`，并绕过直线速度斜坡残留；原有转向位置保持参数和实现已移除。正常路径跟踪与航点位置恢复仍保留直线运动能力。

### P1-16 串口 RPY 过期与速度通道没有统一处理

RPY 数据过期时，串口桥可能只停止发送 RPY，速度仍继续发送。若 RPY 对底盘闭环不是必需，这样可以保住完赛；若底盘把 RPY 当作必需控制量，就会出现上位机认为还能跑、下位机实际控制不完整的分裂状态。

应在协议层明确 RPY 是否是运行门禁，至少让诊断状态说清楚“速度仍在发，但姿态反馈已过期”。

### P1-17 IMU 参数的 finite 校验缺失

相关代码：`src/imu_rpy_filter/src/inputs.cpp:16-31`。

`declare_positive()` 和 `declare_nonnegative()` 只检查大小，没有检查 `std::isfinite()`。如果 YAML 或命令行传入 NaN，比较结果可能绕过校验，NaN 会进入滤波器参数和后续计算。

这类配置错误不应让车在比赛中“带病运行”，但也不应在运行中把一次坏参数变成隐蔽的导航故障。启动时应明确拒绝并提示参数名；普通传感器坏样本则继续按降级策略处理。

### P1-18 [已解决] 实车路线与界面路线看起来左右相反

第一次结论不完整。界面确实存在显示旧预览路线的问题，但最新实车 bag 又确认了第二个问题：ORB 在 `39.416 s` 原地转向期间单帧横跳约 `15 cm`，融合门短暂拒绝后又把新坐标当成恢复结果放行，`map -> odom` 随即把融合车位从路线一侧拉到另一侧。`82.079 s` ORB 丢失后恢复时，`visual_raw` 又从约 `(0.237, -0.110, -1.75)` 清到接近 `(0, 0, 0)`，同一问题再次发生。

bag 中正负角速度命令、左右轮差、IMU 和视觉 yaw 仍然同号，因此没有证据支持盲目翻转电机或规划器 Y 轴。实际造成画面上“车与路线反向”的，是活动路线显示错误和视觉恢复后错误改写 `map -> odom` 两件事。

处理状态：本轮已解决。前端只显示已经 ACK 的活动路线；融合校正节点按视觉测量时间识别中断，并在恢复首帧把新视觉原点锚到当前局部推算的全局车位，只沿用后续视觉增量，不再让恢复帧把 `map -> odom` 拉回 ORB 零点。用本次 bag 重放后，第一次转向处不再从约 `(0.18, 0.01)` 跳到 `(0.07, -0.08)`，ORB 后续清零也没有再把融合车位拉回原点。没有反转 Y、IMU 或左右轮符号。

## 3. 其他会降低成功率的风险

### P2-01 IMU 姿态核心仍是欧拉角一阶积分

相关代码：`src/imu_rpy_filter/src/update.cpp:81-106`。

在大俯仰、颠簸和强线加速度下，`tan(pitch)` 形式对噪声敏感，接近俯仰奇异位置时还要人为截断。比赛场地若基本平整，这不是当前第一优先级；但应通过实车数据确认不会给 yaw rate 造成明显跳变，不要把它当作已经严格解决。

### P2-02 map 校正的协方差只是近似

`/odometry/fused` 的位姿和发布协方差并非完整的联合估计，当前主要把局部和视觉对角方差相加，没有旋转到同一坐标系，也没有交叉相关项。它可能影响融合权重和故障判定，但不应在比赛前为了“数学完整”阻塞主要控制修复；先用实车看是否造成明显跳变。

### P2-03 视觉恢复计数仍可能跨异常样本累计

大多数坏帧会清零恢复计数，但恢复期间再次出现坏增量、tracking 丢失或超时的节点级组合没有完整覆盖，可能凑够所需样本而过早恢复。应确保恢复样本真正连续有效；同时不要因为单个坏帧把整场任务锁死。

### P2-04 map-change 宽限与具体视觉帧没有绑定

`/orbslam3/map_change` 和视觉位姿是两个 topic，没有共同事件 ID。调度顺序变化时，跳变宽限可能用到不对应的帧。当前没有证据证明已在比赛中造成故障，优先级低于启动、转向和失速问题。

### P2-05 ROS 时间与 steady clock 混用

导航周期使用 ROS clock，部分健康和融合计时使用 `steady_clock`。仿真 `/clock` 暂停、回退或跳变时，等待时间和健康超时可能不一致。实车通常影响较小，但所有比赛运行都应使用一致的时间策略并做一次暂停/跳变测试。

### P2-06 [已解决] planner 阶段起点不一定是车辆真实当前位置

前端现在每次阶段续规划都读取最新、经过 frame/时间戳/有限值校验的融合位姿，不再拿上一阶段预览路线末点冒充车辆位置。里程计暂时不新鲜时会进入自动等待探测，恢复后再从真实位置请求下一段；完整赛道上的定位漂移仍需实车验证。

### P2-07 planner 的转弯半径和隧道约束偏软

最小转弯半径主要参与 smoothing，tracking margin 主要参与代价，隧道 entry/exit 更多是顺序目标。它们不是车体一定能通过的硬保证。实际可行性应靠 footprint 栅格检查和实车路线验证，不能只看规划返回 `success`。

### P2-08 GUI hold 可能不释放

GUI 关闭时没有可靠释放自身 motion hold，可能留下永久停车。应在关闭、异常退出和重新连接时发送 release；同时保留一个明确的人工释放入口。

### P2-09 启动脚本有较强的“先杀再检查”行为

启动脚本会先停止已有 ROS 进程，再做部分相机预检；预检失败时原来的可用栈已经被杀掉。进程扫描规则也较宽，可能误杀同一环境的其他 ROS/遥控节点。应先完成关键预检，再停止旧栈；进程匹配要限定到本次运行的 PID/manifest。

### P2-10 启动锁和构建锁的时序存在竞态风险

构建锁、运行锁和进程清理的获取顺序不完全统一，两个启动脚本同时运行时可能互相等待或误判。比赛前固定单一启动入口即可降低风险，长期再统一锁协议。

### P2-11 当前测试不能代表比赛能力

目前选定包构建成功，本轮相关测试汇总 264 项通过；但缺少真实相机、底盘、串口、长路线、动态障碍、失视觉、IMU 丢帧、低速卡死和进程退出测试。当前工作树也有大量用户改动、删除和未跟踪文件，复现环境不够干净。

通过单元测试只能说明若干函数符合预期，不能说明整车能坚持完赛。至少要补一条完整路线和一组故障注入测试。

### P2-12 [已解决] planner 的阶段状态和前端记账曾可能制造“看起来成功”的结果

本轮已经把三个概念拆开：`success` 只表示本次得到合法规划结果，`navigation_activated` 表示路线确实发布给导航，`all_targets_reached` 才表示整个任务已完成。部分目标暂时不可达时，planner 保留已经成功的安全阶段并列出 `deferred_targets`；只要该阶段能完成 `remaining_visits` 中的新任务或新增真实道路覆盖，就允许先跑，不能新增进展的连接线或重复路线不会激活。

前端只在匹配 route ACK、看到本路线进入有效运行状态并收到 `GOAL_REACHED` 后，才把预测的任务、隧道和道路写入已完成集合。然后从最新融合位姿自动续规划。规划失败、暂时没有新进展或 odom 尚未恢复时进入 2/3/4.5/5 秒有界退避探测；路线 1.5 秒没有确认时丢弃 pending 并自动重新请求，迟到 ACK 不会误激活旧状态。与路线有关的故障只让相同路线短时冷却约 5 秒，不会永久封禁它。

仍需保留三个实际边界，但它们不再属于“假成功”问题：

- 地图、配置或依赖在进程启动前就有错误时，planner 仍可能无法启动；这是启动自检问题，不能靠一次服务响应恢复；
- planner service 请求已有 30 秒超时看门狗；超时后取消旧 future、释放 `in_flight` 并继续处理最新请求，不会因为一次后端卡住永久停止重规划；
- 进度按整段路线完成后一次提交。中途已经路过某任务但随后故障时，本段不会记账，下一轮可能重复跑；这是宁可重复、不能虚报的比赛取舍。上述闭环已由单元测试覆盖，但完整赛道、真实相机、底盘和串口仍需实车验证。

### P2-13 ORB 的帧同步、图像失败和 tracking 状态缺少统一恢复策略

已确认的边界行为：

- 左右图像时间差超过约 10 ms 会丢弃 stereo pair；
- 图像转换失败、tracking 未初始化、状态为 3/4/LOST 时不发布有效 pose；
- 长时间没有图像时 ORB 不一定主动发 LOST，最终要等 fusion freshness 过期；
- tracking 重新建立原点后旧路线可能不再对应；
- wrapper 或关键节点退出后只发零速、停止剩余栈，没有自动拉起。

单帧丢弃是合理的，问题是上层没有把这些情况统一成“短暂等待、低速恢复、超过预算再重规划”。应补状态超时和恢复事件，不要让每个模块各自决定是否结束任务。

### P2-14 编码器兼容、重复帧和轮速告警需要统一口径

编码器字段不是 4 个会被忽略；旧式三字段帧会补字段，但 sequence 可能始终为 0，导致后续被当成重复帧；重复帧不刷新 freshness，最终轮速过期；sequence 缺口通常继续并增加不确定度；轮速打滑或编码器故障在视觉健康时多半只告警。

这里不应为了“协议严格”直接停止整车。应先尝试识别旧协议、建立新的 baseline；只有确认轮速是当前唯一可信定位源且已失效时，才停车或进入有限盲推。

### P2-15 生产地图、frame 和成功容差需要一次性冻结

planner 输出通常是 `map`，occupancy grid 使用 `arena`，没有自动 TF；planner 约 `0.08 m` 的任务容差和 navigator 约 `0.04 m` 的航点容差也不一致。仿真时间没有 `/clock` 时，动态路线时间戳可能为 0，导航会直接忽略。

这类问题适合在比赛前一次性检查并写入启动自检，不适合在比赛过程中靠人工解释。路线能否接收、能否到点、是否算完成，必须使用同一套 frame 和容差。

### P2-16 planner 前端把服务名写死

`scripts/arena_route_frontend.py:30-61` 固定连接 `/arena_path_planner/plan`，而 planner 服务名来自地图配置的 `topics.service`。默认配置目前相同，但换测试地图或比赛配置后，planner 可能已经正常运行，GUI 却一直提示后端未就绪。

这不会直接让车撞东西，但会在比赛现场表现为“规划按钮坏了”。前端应读取同一份配置或从参数传入服务名，连接失败时明确区分服务名不匹配和 planner 真正退出。

## 4. 按实际情况应怎样反应

下面是比赛版本应努力达到的口径。它不是“看到异常就停”的清单。

| 情况 | 当前/期望动作 | 是否应人工介入 |
|---|---|---|
| 没有路线 | 零速等待 `WAITING_FOR_ROUTE`，收到合法路线后自动接着做 | 否 |
| CSV 路线不存在或格式错误 | 报 `FAULT_ROUTE_NOT_LOADED`，不盲跑 | 修好文件后重启 |
| 合法路线但 `autostart=false` | 保持 `IDLE` 零速，等待一次 `start` | 需要发 start |
| ORB 正在暖机、尚未有第一帧有效视觉 | 零速等待初始化，初始化超时才报故障 | 不应立刻人工 |
| IMU 单帧坏、时间戳偶发异常 | 丢该帧，继续或短时降速 | 否 |
| IMU 长时间失效、视觉正常 | 约 65% 速度继续 | 否 |
| 视觉短暂中断且 wheel+IMU 可信 | 约 50% 速度、受时间/距离预算限制 | 否 |
| 视觉恢复但协方差还在渐变 | 约 50% 速度，逐步恢复正常 | 否 |
| 只有视觉、没有 IMU 或 wheel | 按配置降速，前提是状态明确允许 | 否 |
| 只有 wheel、没有视觉和 IMU | 默认 `fuse_wheel_yaw=false`，不会放行 `DEGRADED_WHEEL_ONLY`，先停车等待；只有重新标定并显式开启后才考虑低速短桥接 | 超预算或确认设备失效后人工 |
| fusion status 未知、过期或未允许 | 先短暂停车等待，不要立即锁死 | 持续超时才人工 |
| 视觉中断超预算或坐标明显跳变 | 停车等待重定位，必要时重规划 | 持续失败才人工 |
| 横向误差/曲率变大 | 连续降速，仍沿路线走 | 否 |
| yaw 误差较大 | 低速原地转向 | 否 |
| yaw 已进入容差 | 连续稳定约 0.30 秒后继续 | 不应因计时 bug 停车 |
| 普通航点已通过且仍在走廊内 | 推进到下一航点 | 否 |
| 需要停车的航点、停稳确认成功 | 等待 `stop_time` 后继续 | 否 |
| 路径 10 秒无进展但车仍有小幅可信移动 | 先重置控制器，再尝试 | 否 |
| 发出运动命令但车完全不动 | 短暂停车，检查执行器/定位，自动重试 | 重试失败才人工 |
| 连续路径恢复仍失败 | 局部重规划或锁存零速 | 需要人工处理障碍/设备 |
| 转向误差 6 秒无改善 | 停车、重新对齐或换策略，再试 | 两次失败后人工 |
| 需要停车的航点 | 零速，确认速度低于阈值并等待 | 否 |
| odom 暂时无效但底盘已停 | 短等传感器恢复，不立即误报刹车故障 | 否 |
| 刹车确认超出单次时间窗 | 保持零速并自动重新确认；轮子仍在转时不进入转向 | 不需要人工 |
| 串口短时断开/CTL 丢一帧 | 零速短等并自动重连 | 否 |
| 串口连续恢复失败、急停或 MCU 非导航模式 | 持续零速，有限重试后锁存 | 需要人工 |
| RPY 过期但速度链路正常 | 按协议决定继续或降速，同时明确告警 | 通常不需要 |
| planner 一次请求失败 | 保留旧路线并告警，或在确认旧路线失效时停车重规划 | 视情况 |
| 动态障碍挡路 | 低速停车、等待/自动重规划 | 尽量不需要人工 |
| hold | 零速，所有来源释放后自动继续 | 否 |
| `stop`/`reset`/任务完成 | 零速，下一次运行需要重新 `start` | 需要人工发命令 |

## 5. 是否存在严重的防御性编程问题

### 结论：存在，而且是“有些地方过度防御，有些地方反而防得不够”。

严重之处不在于代码检查了 NaN、frame 或时间戳，而在于它把很多**可恢复的短暂异常**直接映射成了**不可逆的任务终止**：

1. **[已解决] 初始化状态被当成运行故障**：ORB 暖机现在进入可自动恢复的初始化等待，不再直接锁存任务。
2. **健康状态层级不够细**：未知、过期、未初始化、短暂丢帧和已确认故障在多个路径上最终都走停车/锁存，没把“等一会儿”和“任务失败”分开。
3. **重试动作不改变条件**：路径/转向恢复只是 reset 同一控制器，次数用完就锁存，像是在重复按同一个按钮。
4. **[已解决] 传感器暂时无效会否定真实运动**：刹车判停已改为新鲜 MCU 双轮速度优先、融合位姿回退，超时自动重试，不再结束任务。
5. **坏消息静默丢弃、旧任务继续**：动态路线无效或 planner 失败时，旧路线可能继续跑，系统既没有明确确认，也没有给出足够强的告警。
6. **进程退出偏向结束整场任务**：大多数节点没有自动拉起和状态恢复，偶发软件问题直接变成人工介入。
7. **启动脚本先清场再检查**：预检失败会把原来能工作的栈先停掉，属于操作层面的过度破坏。
8. **[已解决] planner 输入防御不足**：坏请求现在被拒绝并返回失败，planner 进程和上一条路线都保留。
9. **[已解决] planner 失败时仍输出目标点**：A* 连接失败不再输出未经验证的直线，最终路线还会逐段复核。
10. **[已解决] 覆盖规划可能假报成功**：完整 tour、实际道路覆盖和回程分别核对，部分结果不会冒充完整任务激活。
11. **[部分解决] 配置参数防御不足**：导航 odom 的 frame、child frame 和有限值已补齐；IMU 参数的 NaN 校验仍待处理。

同时也有一些**不应削弱**的检查：

- NaN/Inf、明显非法四元数和不可能的 frame；
- MCU 急停、非导航模式、命令通信完全失效；
- 已确认的机械失速；
- 视觉丢失超过明确的时间/距离预算；
- 路线坐标系已经确认改变且无法重对齐。

这些检查的目标不是让系统“看起来严谨”，而是防止车辆在完全没有可信反馈时继续消耗路线。真正需要改的是**异常分级和恢复路径**，不是把所有检查删掉。

另外，轮速不进入 EKF 并不是防御性编程过度，而是基于实车漂移数据做出的工程选择。不能为了让传感器数量看起来更多，就把明显不稳定的轮速作为高置信度状态输入；应把它限制在监督、失速判断和经过标定的短时辅助范围内。

### 建议采用的实际故障分级

```text
正常 RUN
  -> 传感器/执行器短暂异常：SLOW
  -> 信息暂时不可信：HOLD_AND_WAIT
  -> 自动重对齐/重连/局部重规划：RECOVER
  -> 有证据确认无法继续：LATCHED_FAULT
```

特别注意：启动阶段要有独立的 `INITIALIZING`，不能复用运行阶段的 `FAULT`；运行阶段的 `FAULT` 也不能因为“想完赛”而全部降级成盲跑。

## 6. 比赛前最实际的处理顺序

1. [已完成] P0-01：ORB 初始化等待和真正故障分离。
2. [已完成] P0-02：转向 settling 计时器幂等。
3. [已完成] P0-03：IMU 大间隔后重新建立时间基线。
4. [已完成] P0-04：增加完全不动的低速/零位移超时。
5. 实车标定 D-01：失视觉时允许继续多远、以多低速度继续；不要因为测试需要强行把飘移轮速加入正常 EKF。
6. [已完成] P0-06：独立核对地图全覆盖、隧道、回程和“无剩余任务”返回值。
7. [已完成] P0-08/P0-09：planner 坏输入不能崩溃，A* 失败不能输出未验证路线，coverage 不能假报完成。
8. [已完成] P0-07：固定比赛启动入口，确认执行器健康门禁实际开启。
9. [已完成] P0-10：动态障碍和导航 odom/IMU 的坏输入可恢复处理。
10. [代码已完成，待实车复跑] 用最坏工况验证停车确认，并跑一次完整路线。
11. 做四组故障注入：相机暖机/丢跟踪、IMU 丢帧、轮子卡死、串口断开。
12. 最后再处理协方差、时间抽象和代码结构等不会直接决定本场完赛的问题。

当前底线判断：**工程已经有可用的安全监督骨架，但仍存在会让车过早停下、永远等不到恢复、或错误宣布完成的实质问题。比赛策略应是“能继续就继续，能降速就降速，能自动恢复就自动恢复；只有证据充分时才锁存”，而不是简单地把所有异常都变成停车。**
