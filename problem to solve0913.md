# problem to solve 0913：实车低速纠偏、转弯和到点卡住

更新时间：2026-09-13。目标不是让车见到误差就停，而是让它在有可信定位和可控底盘时持续纠偏、自动走完路线。本文只记录本次对话中依据最近 bag 和当前代码确认或发现的问题；不把推测写成实车结论，也不重复 `problem to solve2.md` 的全部历史清单。

## 先说结论

1. **明确的上位机控制 bug：**车已经因横向偏差降到约 `0.10 m/s`，Stanley 纠偏公式仍使用降速前的目标速度 `0.40 m/s`。角度误差和横向修正因此几乎抵消，纠偏角速度长期很小；不是下位机拒绝执行 `0.10 m/s`。
2. **当前代码的终点门槛冲突，且与 bag 高度吻合：**在横向误差无法进入 `4 cm` 到点圈时，车于纵向剩余约 `1.5 cm` 被制动到零，却必须再走到 `1 cm` 内才能按“沿线通过”到点。末端反复在 `FOLLOWING` / `BRAKING_APPROACH` 间切换。
3. **低速精细转向的响应/监督门槛值得修：**末尾命令约 `0.20 rad/s`，实际角速度很低；融合层的角向失速检查却要命令达到 `0.55 rad/s` 才启动。导航还有独立的转向进度监督，不能说“完全没监督”，但此次最后几度明显耗时。

不能从这些数据直接断言车身在地面上真偏了 `5~6 cm`。这里的横向偏差是**导航收到的融合位姿相对目标路线的偏差**；视觉定位是否也有偏差，要单独验证。

## 使用了哪些记录

最近保留的三个 bag 位于 `navigation_runs/`，录制时间均为 2026-09-12（目录名时间为 UTC）：

| bag 目录前缀 | 时长 | 是否有这次需要分析的转弯 |
|---|---:|---|
| `20260912T100135` | `30.8 s` | 只有 `WAITING_FOR_ROUTE`，运动和转向命令均为零。 |
| `20260912T100213` | `27.6 s` | 同样在等待路线，没有实际转弯。 |
| `20260912T100251` | `85.4 s` | 有一次约 90 度转向和后续直行，是以下数字的来源。 |

第三个 bag 的路径 `/waypoint_path` 只有两个点：`map (2, 0) -> (2, 3)`。导航开始时从当前位置走向 `(2, 0)`，随后转约 90 度沿 `x=2` 直行。因此**这次转弯后长时间低速不是“规划点太密”**，不能拿这段记录证明密集点/PID 重置是本次降速原因。

第三个 bag 中，以录制开始为 `t=0`：

| 阶段 | `/cmd_vel_nav` 平均线速度 | MCU 双轮速度平均值 | 现象 |
|---|---:|---:|---|
| `t=29~32.2 s`，转弯前直行 | `0.367 m/s` | `0.319 m/s` | 命令最高到 `0.40 m/s`。 |
| `t=32.2~33 s`，进弯减速 | `0.212 m/s` | `0.303 m/s` | 轮子有制动滞后。 |
| `t=33~46.5 s`，原地转向 | `0 m/s` | 转起来后接近零 | 融合 yaw 由约 `-1.5` 度到 `89.4` 度。 |
| `t=46.5~48.5 s`，出弯头两秒 | `0.170 m/s` | `0.209 m/s` | 曾短暂下发 `0.314 m/s`。 |
| `t=52~69 s`，后续直行 | **`0.100 m/s`** | **`0.104 m/s`** | 下位机基本跟随上位机的慢速命令。 |

该次运行的 manifest 在 `navigation_runs/20260912T100251.685458Z_cb2767eff67f/run_manifest.json` 中记录：`/waypoint_navigator` 参数快照抓取超时，`parameter_snapshots_complete=false`。当前 YAML 与运行时配置文件的哈希已经不同，所以以下关于**当前参数**的分析要与 bag 表现一起看；尤其终点门槛冲突应做一次定向实车复现，不能假装已经拿到了那次运行的全部参数。

## 0913-01：跟线纠偏用了固定目标速度，没有用实时执行速度 [代码已修，待实车]

**证据：**目标直线为 `x=2.000 m`。融合位姿在转完后约 `x=1.985 m`，`t=52 s` 约 `1.945 m`，`t=60 s` 约 `1.941 m`，`t=69 s` 约 `1.946 m`。也就是导航看到横向误差持续约 `5~6 cm`。当前配置 `cross_track_slowdown_start=0.015 m`、`cross_track_slowdown_full=0.04 m`、`cross_track_minimum_speed=0.10 m/s`，所以超过 `4 cm` 后把直行限到 `0.10 m/s` 与 bag 完全吻合。

**代码原因：**`src/visual_navigation/src/path_following.cpp` 的 `RunPath()` 先取 `requestedSpeed=min(waypoint.speed, maxLinearSpeed_)`，把它传给 `StanleyPathError()`；之后才调用 `CrossTrackSpeedLimit()` 算实际下发的低速。`StanleyPathError()` 位于 `src/visual_navigation/include/visual_navigation/path_control.hpp`，速度进入横向修正项的分母。导航明明已在 `src/visual_navigation/src/runtime.cpp` 接收 `/odometry/fused.twist.twist.linear.x`，刹车和进度监督也在用它，只有这里仍拿目标速度计算。

用此 bag 的中位数举例：融合 `x=1.9443 m`、yaw `82.18` 度，目标朝向 `90` 度；横向差 `5.57 cm`、航向差 `7.82` 度。按目标速度 `0.40 m/s`、当前增益 `1.50` 和软化速度 `0.25 m/s`，横向项约 `7.33` 度，剩下仅 `0.49` 度误差；若按约 `0.10 m/s` 的实速计算，横向项约 `13.43` 度。`t=52~69 s` 期间，约 **81%** 的转向命令绝对值小于 `0.05 rad/s`，平均角速度命令约 `-0.004 rad/s`，融合位姿中的横向差没有收敛。

**影响：**车一偏就降速；降速后纠偏角却仍按快车计算；由于这次两项近乎抵消，它长期低速且离路线约 `5~6 cm`。这不是下位机不能跑 `0.1 m/s`，因为 MCU 实测约 `0.104 m/s`。

**已做：**Stanley 现在使用本控制周期经过横偏限速、曲率限速、接近制动、融合健康降速和加减速斜坡后的计划线速度，不再使用写死的巡航速度。轮式位置没有重新接入融合；融合速度只继续用于刹车反馈和失速监督。直线参数同时从 `Kp=1.80/Ki=0.15/横偏增益=1.50/最大拦截角=0.70` 调到 `1.20/0.08/1.00/0.30`，用于减少来回过冲。

**验收：**同一条先沿 X 走 `2 m`、再沿 Y 走 `3 m` 的路线，出弯后若融合横向误差持续超过 `4 cm`，控制器应有有方向、能被 IMU 观察到的纠偏，而不是连续十几秒把 `0.1 m/s` 和接近零的角速度当作正常结果。若纠偏命令有效但融合 X 仍不变，再调查定位，而不是继续盲加 PID。

## 0913-02：终点制动余量与到点判定冲突 [待修，需复现]

**证据：**目标为 `(2,3)`；`t=71~74.3 s` 融合位置大约在 `x=1.942~1.944`、`y=2.984~2.986`，即横向差约 `5.6~5.8 cm`、纵向距终点约 `1.4~1.6 cm`。此时速度命令基本在 `0~0.007 m/s`，没有 `GOAL_REACHED`，`FOLLOWING` 和 `BRAKING_APPROACH` 在 `t=69.82~74.25 s` 之间切换约 28 次。

**当前代码/参数为何会卡：**`src/visual_navigation/config/waypoint_navigation.yaml` 当前给出 `braking_safety_margin=0.015 m`、`waypoint_tolerance=0.04 m`、`waypoint_pass_longitudinal_tolerance=0.01 m`、`waypoint_pass_lateral_tolerance=0.06 m`。`src/visual_navigation/include/visual_navigation/path_control.hpp` 的 `BrakingSpeedLimit()` 在剩余距离不大于 `1.5 cm` 时把可用制动距离归零；`WaypointReached()` 则要求**径向小于 `4 cm`**，或**纵向剩余不超过 `1 cm` 且横向差不超过 `6 cm`**。当横向差约 `5.7 cm` 时径向条件失败，车却可能在纵向还剩约 `1.5 cm` 就被制动，不再走到 `1 cm` 的通过线。`src/visual_navigation/src/path_following.cpp` 用投影剩余距离作刹车距离，和 bag 的停点相吻合。

**影响：**车明明接近终点，却一直不判完成；毫米级位姿抖动导致“跟随/刹车”来回切换。`t=74.29 s` 串口另外报告 `DISCONNECTED`，导航随后进入 `WAITING_FOR_ACTUATOR_RECOVERY`；串口断开是后续独立事件，不应拿它替代上述终点卡住的原因。

**修复方向：**把到点、制动余量、纵向通过门槛统一起来：例如在确认前方通道仍允许且定位连续时，提供受控的最后几毫米低速接近；若横向误差不符合到点条件，就进行有限的安全纠偏/重规划，不应把制动到零当成已经到点。不能简单把所有到点阈值调大并谎报完成，也不能取消刹车让车冲过障碍。给 `x=1.944, y=2.985, target=(2,3)` 加状态机回归测试，并用实车短路线复现确认那次运行的实际参数。

**验收：**可到达的终点能自动通过到点条件并报告完成；暂时无法精确到点时，状态能稳定地选择小幅纠偏、等待定位或重规划，不在 `FOLLOWING` 与 `BRAKING_APPROACH` 之间无限跳转。

## 0913-03：转弯最后几度的最小有效转速与失速门槛错配 [待标定]

**证据：**`t=33 s` yaw 约 `-1.5` 度，`t=39 s` 已到 `83.4` 度；到 `t=46.5 s` 才到 `89.4` 度。也就是说大部分转弯约 6 秒完成，最后约 6 度又耗了约 7.5 秒。`t=39~46.5 s` 命令角速度中位数约 `+0.20 rad/s`，IMU 角速度中位数约 `+0.01 rad/s`，MCU 轮速也基本接近零。

**门槛差异：**当前 `min_precision_turn_speed=0.20 rad/s`，而 `src/fused_odometry/config/fused_odometry.yaml` 的 `angular_stall_command_threshold_radps=0.55`。所以这种“精细转弯命令发了，底盘几乎不动”的状态不会触发融合层的角向机械失速判定。导航另有按航向误差改善计算的 `TurnProgressSupervisor`（约 6 秒无足够改善才尝试恢复）；最后几度若偶尔有少量进展，可能仍耗较长时间。这条尚不能仅凭 bag 断定是电机、电控、地面摩擦还是角度估计的哪一种问题。

**修复方向：**上位机控制与下位机一起量出能稳定起转的最小角速度及对应实测 yaw rate，在精细转向时避免命令落在持续无响应的区间；仍要有靠近目标时的减速/防过冲策略。对 `0.20~0.45 rad/s` 低速转向建立“命令持续存在但 IMU 与轮反馈没有相应响应”的诊断，不要只依赖 `0.55 rad/s` 的高命令失速门槛；有限尝试后可以换转向策略，不要反复空等同一个动作。

**验收：**分别测试 `45` 和 `90` 度转向，记录角速度命令、IMU、双轮反馈和末尾误差；末尾几度不应再无有效转动地耗数秒，也不能为了追求快而大幅过冲、在窄路口横移。

## 与本次问题相关，但还不能下结论的事

- **真实横向偏移还是融合坐标偏移：待分辨。**`/odometry/visual_continuous` 与 `/odometry/fused` 在 `t=52~69 s` 的 `x` 走势接近，说明融合位置大体跟着视觉估计；这不是地面真值。`/wheel/odom` 漂移已知，不能拿它的绝对位置否定视觉，也不能强行并入 EKF。可用固定参照物/场地测量和完整的 ORB、map->odom、IMU 记录对照实际车头及车位。无论地面是否真偏，0913-01 的速度口径错误仍然存在。
- **串口在 `t=74.29 s` 断开：独立事件。**`/cup_car_serial/connected` 从 true 变 false，跟踪状态报告 `DISCONNECTED`，`t=74.32 s` 导航进入 `WAITING_FOR_ACTUATOR_RECOVERY`。仅凭这些字段不能把断连归咎于纠偏、终点逻辑或底盘硬件；排查需看串口错误、MCU 重启及连接恢复。普通短断连应停车后自动恢复。
- **历史上同类“口径不一致”已修过：**停车判停曾用视觉小幅回摆代替 MCU 双轮停稳反馈，出现 `FAULT_WAYPOINT_BRAKE_TIMEOUT`；原地转向曾夹带微小线速度，导致下位机退出专用原地转向模式。详见 `problem to solve2.md` 的 P1-03、P1-15。不要因为这些条目已标解决，就把当前三个新问题也当作已经解决。

## 建议的处理顺序

1. 修跟线速度口径并做低速横向偏差回归测试。这直接决定转弯后能否回到路线。
2. 对齐终点制动与到点状态机，用原来 `(2,0)->(2,3)` 的短路线复跑，确认能自行完成而非在终点前抖动。
3. 测定下位机最低有效转速后改精细转向和失速门槛。全过程记录命令、融合位姿、IMU、双轮速度和状态，不需要录图像。

## 0913-04：最新整图运行的直线蛇形 [代码已调，待实车]

最新完整 bag 为 `navigation_runs/20260913T025555.013983Z_cb2767eff67f/bag`。其中 `80.10~104.27 s` 是一条约 `3.8 m`、没有中间航点的直线；融合横偏为 `-5.2~+2.1 cm`，航向为 `-7.0~+12.7 度`，上位机角速度命令在 `-0.273~+0.272 rad/s` 之间反向 24 次。因此这次打摆子不是路径点太密，而是上位机纠偏来回过冲。已降低上述直线纠偏增益、最大拦截角和小误差强制转向，实际改善量必须看下一次 bag。

## 0913-05：路口掉头和转到位后仍补转 [上位机已修，下位机待修]

本包四次原地转向的车身平移约为 `14.0、20.6、11.4、14.6 cm`。第二个 90 度弯在 `74.73 s` 已到道路方向约 `+0.7 度`，上位机仍发约 `-0.20 rad/s` 到 `79.94 s`；所以肉眼看到的左轮多转，不是停令之后莫名失控，而是当时上位机还在追逐随横偏变化的 Stanley 斜切方向。

上位机现已改为：只有在计划拐点停车完成后才允许原地转；转向期间锁定道路方向，不追横向漂移形成的斜切方向；误差进入约 `5.2 度` 后立即发零命令，并同时等待 IMU 和新鲜双轮反馈确认停稳；180 度掉头拆成两个 90 度，中间明确停车一次；普通直线路段出现大角度异常时不原地掉头，而是沿实走轨迹倒退并要求重规划。最终航点的切线 yaw 不再授权路中原地调整。

但下位机问题仍然存在：四次转向的左右编码器总增量分别约为 `+1411/-111、+3318/-8、+1226/-391、+1803/-19`。180 度转向期间右轮收到反向 PWM，但 106 个有效样本中没有一次反向轮速超过 `0.02 m/s`。也就是说，右轮反转基本没有起步，左轮把车推着平移。上位机减少无效补转后会好一些，但不修右轮反转，仍无法可靠完成 90 度原地转向。

## 0913-06：规划器可能在路中安排转向 [正在修]

现有规划器只判断原地转时车身是否碰撞，没有强制大角度转向点属于 `inspection_graph` 路口。第三阶段还能产生离图节点约 `9 cm` 的路中转角；最新 bag 还出现相隔约 `1 cm` 的两个假拐点，肉眼上等于连续掉头。规划器正在增加“正式图只在真实图节点/入口路口转向”的约束并清除 1 cm 假折点。整图三阶段回归全部通过前，不应拿当前中间版本直接上车。

本文中的“代码已修”只代表编译和自动测试通过，不等于整车已经复测。最终验收仍是：**无需人工纠偏、尽可能持续完赛**，并以新 bag 的实际轨迹为准。
